import 'package:flutter/material.dart';

class AuthScreenRedesigned extends StatelessWidget {
  const AuthScreenRedesigned({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('New Auth Screen'),
      ),
    );
  }
} 